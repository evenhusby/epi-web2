---
import { t, type Lang } from '../lib/i18n';

interface Props {
  lang: Lang;
}
const { lang } = Astro.props;
---

<div class="wrap">
  <div class="hero">
    <h1>{t(lang, 'hero_h1')}</h1>
    <p>{t(lang, 'hero_p')}</p>
  </div>
</div>
